import 'package:flutter/material.dart';
class LoginPageFlutterFlow extends StatelessWidget {
  const LoginPageFlutterFlow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}